# render

Converts the provided `RuntimeFlags` into a `string`.

To import and use `render` from the "Flags" module:

```ts
import * as Flags from '@effect/io/Fiber/Runtime/Flags'

// Can be accessed like this
Flags.render
```

**Signature**

```ts
export declare const render: (self: RuntimeFlags) => string
```
