# make

Creates a new `FiberId`.

To import and use `make` from the "Id" module:

```ts
import * as Id from '@effect/io/Fiber/Id'

// Can be accessed like this
Id.make
```

**Signature**

```ts
export declare const make: (id: number, startTimeSeconds: number) => FiberId
```
