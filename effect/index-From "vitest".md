# From "vitest"

Re-exports all named exports from the "vitest" module.

To import and use `From "vitest"` from the "index" module:

ts
import \* as index from "@effect/vitest/index"
// Can be accessed like this
index.From "vitest"
undefined
