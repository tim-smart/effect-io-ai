# void

Create a resolver that performs side effects.

To import and use `void` from the "Resolver" module:

```ts
import * as Resolver from "@effect/sql/Resolver"
// Can be accessed like this
Resolver.void
```
