Package: `effect`<br />
Module: `Schema`<br />

## Schema.DateTimeUtc

Defines a schema that attempts to convert a `string` to a `DateTime.Utc` instance using the `DateTime.unsafeMake` constructor.

**Signature**

```ts
declare class DateTimeUtc
```

[Source](https://github.com/Effect-TS/effect/tree/main/packages/effect/src/Schema.ts#L6932)

Since v3.10.0