# Failure

A Micro Failure is a data type that represents the different ways a Micro can fail.

To import and use `Failure` from the "Micro" module:

```ts
import * as Micro from "effect/Micro"
// Can be accessed like this
Micro.Failure
```
