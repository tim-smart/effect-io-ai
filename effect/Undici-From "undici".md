# From "undici"

Re-exports all named exports from the "undici" module.

To import and use `From "undici"` from the "Undici" module:

```ts
import * as Undici from "@effect/platform-node/Undici"
    // Can be accessed like this
    Undici.From "undici"
```
