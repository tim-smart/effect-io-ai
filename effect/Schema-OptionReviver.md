Package: `effect`<br />
Module: `Schema`<br />

## Schema.OptionReviver

Reviver for persisted `Option` declarations.

**When to use**

Use when reconstructing documents that may contain schemas created by `Option`.

**See**

- `Option` for creating the corresponding schema

**Signature**

```ts
declare const OptionReviver: SchemaRepresentation.DeclarationReviver<null>
```

[Source](https://github.com/Effect-TS/effect/tree/main/packages/effect/src/Schema.ts#L9646)

Since v4.0.0