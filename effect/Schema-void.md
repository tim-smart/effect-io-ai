# void

Run a sql query with a request schema and discard the result.

To import and use `void` from the "Schema" module:

```ts
import * as Schema from "@effect/sql/Schema"
// Can be accessed like this
Schema.void
```
