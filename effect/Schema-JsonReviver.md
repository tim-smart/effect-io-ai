Package: `effect`<br />
Module: `Schema`<br />

## Schema.JsonReviver

Reviver for persisted `Json` declarations.

**When to use**

Use when reconstructing documents that may contain the `Json` schema.

**See**

- `Json` for the corresponding immutable JSON schema

**Signature**

```ts
declare const JsonReviver: SchemaRepresentation.DeclarationReviver<null>
```

[Source](https://github.com/Effect-TS/effect/tree/main/packages/effect/src/Schema.ts#L15968)

Since v4.0.0