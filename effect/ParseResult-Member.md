# Member

Error that occurs when a member in a union has an error.

To import and use `Member` from the "ParseResult" module:

```ts
import * as ParseResult from "@effect/schema/ParseResult"
// Can be accessed like this
ParseResult.Member
```
