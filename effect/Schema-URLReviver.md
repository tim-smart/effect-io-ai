Package: `effect`<br />
Module: `Schema`<br />

## Schema.URLReviver

Reviver for persisted `URL` declarations.

**When to use**

Use when reconstructing documents that may contain the `URL` schema.

**See**

- `URL` for the corresponding schema

**Signature**

```ts
declare const URLReviver: SchemaRepresentation.DeclarationReviver<null>
```

[Source](https://github.com/Effect-TS/effect/tree/main/packages/effect/src/Schema.ts#L11750)

Since v4.0.0