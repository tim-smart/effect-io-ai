# abort

Abort the current `Micro` effect.

To import and use `abort` from the "Micro" module:

```ts
import * as Micro from "effect/Micro"
// Can be accessed like this
Micro.abort
```

**Signature**

```ts
export declare const abort: Micro<never, never, never>
```
