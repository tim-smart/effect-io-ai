Package: `@effect/cluster`<br />
Module: `ShardId`<br />

## ShardId.ShardId.Encoded

This is the shape that a shard id has over the wire.

**Signature**

```ts
export interface Encoded extends Schema.Schema.Encoded<typeof ShardId> {}
```

[Source](https://github.com/Effect-TS/effect/tree/main/packages/cluster/src/ShardId.ts#L56)

Since v1.0.0