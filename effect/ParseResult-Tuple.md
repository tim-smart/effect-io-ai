# Tuple

Error that occurs when an array or tuple has an error.

To import and use `Tuple` from the "ParseResult" module:

```ts
import * as ParseResult from "@effect/schema/ParseResult"
// Can be accessed like this
ParseResult.Tuple
```
