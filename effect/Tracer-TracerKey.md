Package: `effect`<br />
Module: `Tracer`<br />

## Tracer.TracerKey

Defines the string key for the active tracer context reference.

**When to use**

Use when you need the raw context key for active tracer lookup in lower-level
tracing code.

**Signature**

```ts
declare const TracerKey: "effect/Tracer"
```

[Source](https://github.com/Effect-TS/effect/tree/main/packages/effect/src/Tracer.ts#L558)

Since v4.0.0