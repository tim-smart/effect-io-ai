# Transform

If `propertySignatureTransformations.length > 0` then `decode` / `encode` are derived.

To import and use `Transform` from the "AST" module:

```ts
import * as AST from '@effect/schema/AST'

// Can be accessed like this
AST.Transform
```
