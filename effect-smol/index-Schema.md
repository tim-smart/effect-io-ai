Package: `effect`<br />
Module: `index`<br />

## index.Schema

Re-exports all named exports from the "./Schema.ts" module as `Schema`.

**See**

- `Schema` — type-level view tracking only the decoded Type
- `Codec` — type-level view tracking both Type and Encoded
- `Struct` — define object shapes
- `decodeUnknownSync` — synchronous validation
- `decodeTo` — schema transformations

**Signature**

```ts
export * as Schema from "./Schema.ts"
```

[Source](https://github.com/Effect-TS/effect-smol/tree/main/packages/effect/src/index.ts#L3334)

Since v4.0.0