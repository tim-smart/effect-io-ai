Package: `effect`<br />
Module: `Graph`<br />

## Graph.isGraph

Returns `true` if a value has the graph runtime type identifier, narrowing
it to a `Graph`.

**Signature**

```ts
declare const isGraph: (u: unknown) => u is Graph<unknown, unknown>
```

[Source](https://github.com/Effect-TS/effect-smol/tree/main/packages/effect/src/Graph.ts#L288)

Since v4.0.0