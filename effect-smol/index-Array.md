Package: `effect`<br />
Module: `index`<br />

## index.Array

Re-exports all named exports from the "./Array.ts" module as `Array`.

**See**

- `make` — create a non-empty array from elements
- `map` — transform each element
- `filter` — keep elements matching a predicate
- `reduce` — fold an array to a single value

**Signature**

```ts
export * as Array from "./Array.ts"
```

[Source](https://github.com/Effect-TS/effect-smol/tree/main/packages/effect/src/index.ts#L119)

Since v2.0.0